apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: "{{ prometheusOperator_name }}-mysqld-exporter"
  labels:
    release: "{{ prometheusOperator_name }}"
spec:
  endpoints:
    - interval: 5s
  selector:
    matchLabels:
      exporter: mysqld-exporter
